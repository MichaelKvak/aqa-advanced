const student = {
	firstName: 'Michael',
	lastName: 'Kvak',
	age: 37,
	courses: ['mathematics', 'history', 'IT'],
};

const bossName = student.firstName;
const bossAge = student.age;

console.log(`boss name is ${bossName} and boss age is ${student.age}`);
